# Banana AI Assistant 🍌

**Banana AI Assistant** คือระบบผู้ช่วย AI อัจฉริยะที่ช่วยให้คุณบริหารจัดการและติดตามงานใน **Azure DevOps** ได้ง่ายๆ ผ่านการพูดคุย (Chat) โดยไม่ต้องเปิดหน้าเว็บ Azure DevOps ไปมาให้วุ่นวาย

สร้างด้วย **Ruby on Rails 8.1** และใช้มาตรฐาน **MCP (Model Context Protocol)** ขับเคลื่อนด้วยสมองของ **Groq (Llama 3)** ที่ถูกจูนให้เป็น Senior Project Manager มืออาชีพ

---

## ✨ ฟีเจอร์หลัก (Features)

*   **💬 Web Chat UI:** หน้าจอแชทที่ใช้งานง่าย รองรับภาษาไทย 100%
*   **🤖 Azure DevOps Integration:** เชื่อมต่อข้อมูลจริง
    *   ดึงรายการงาน (Work Items) แยกตามโปรเจกต์
    *   ดูสถานะงาน (Active, Closed, New)
    *   เช็คข้อมูล **Current Sprint** ได้อัตโนมัติ
    *   ดูว่าใครถืองานอะไรบ้าง (Workload Analysis)
*   **🧠 Smart AI Agent:**
    *   วิเคราะห์และสรุปข้อมูลเป็นภาษาไทย (เหมือนรายงานหัวหน้าทีม)
    *   รองรับคำสั่งลัด (Quick Actions) กดปุ่มเดียวรู้เรื่อง

---

## 🛠️ สิ่งที่ต้องมีเบื้องต้น (Requirements)

*   **Ruby:** 3.4.7
*   **PostgreSQL:** 13+
*   **Keys & Tokens:**
    *   **Groq API Key:** สำหรับสมอง AI ([ขอฟรีที่นี่](https://console.groq.com/))
    *   **Azure DevOps PAT (Personal Access Token):** สำหรับดึงข้อมูลงาน ([วิธีขอ PAT](https://learn.microsoft.com/en-us/azure/devops/organizations/accounts/use-personal-access-tokens-to-authenticate))

---

## 🚀 วิธีติดตั้งและเริ่มต้นใช้งาน (Setup Guide)

### 1. Clone Project
```bash
git clone git@github.com:bestpatcharapon/Banana_Ai_Assistant.git
cd Banana_Ai_Assistant
```

### 2. ติดตั้ง Dependencies
```bash
bundle install
```

### 3. ตั้งค่า Environment Variables (สำคัญมาก ⚠️)
สร้างไฟล์ `.env` ที่ root folder และใส่ค่าตามนี้:

```env
# ตั้งค่า Database (ถ้ามี)
# DATABASE_URL=postgres://user:password@localhost:5432/banana_mcp

# --- AI Configuration ---
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Azure DevOps Configuration ---
AZURE_DEVOPS_ORGANIZATION=ชื่อOrganizationของคุณ (เช่น bananacoding)
AZURE_DEVOPS_PAT=วางTokenยาวๆที่นี่โดยไม่ต้องแปลงBase64
```

### 4. เตรียม Database
```bash
bin/rails db:prepare
```

---

## ▶️ วิธีรันโปรแกรม (Usage)

1.  **Start Server:**
    ```bash
    bin/dev
    # หรือ
    bin/rails s
    ```

2.  **เข้าใช้งานผ่าน Browser:**
    *   เปิด **[http://localhost:3000/chat](http://localhost:3000/chat)**
    *   🎉 เริ่มคุยกับ **Banana AI** ได้เลย!

---

## 💡 ตัวอย่างคำสั่งที่ใช้ได้

*   *"ใน bananacoding มีโปรเจกต์อะไรบ้าง"* (ดูรายชื่อโปรเจกต์)
*   *"Banana Test Engineer มีใครทำงานบ้าง"* (เช็คงานในโปรเจกต์ เจาะลึกทีมและงาน)
*   *"สรุปงาน sprint นี้ให้หน่อย"*

---

## 🏗️ Tech Stack

*   **Framework:** Ruby on Rails 8.1 (API Mode + Custom Views)
*   **Language:** Ruby 3.4.7
*   **AI Model:** Llama-3 (via Groq API)
*   **Architecture:** MCP (Model Context Protocol) Implementation
*   **Database:** PostgreSQL

---

Developed with ❤️ by **Banana Coding Team** 🍌
